#include <iostream>

int main() {
    bool isBlocked = false;

    if(!isBlocked) {
        std::cout << "You can send message";
    } else {
        std::cout << "You are blocked";
    }

    return 0;
}